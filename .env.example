# AI Omnichannel Sales OS Web — Environment Configuration
# Copy to .env.local for local development.
# NEVER commit .env.local or any file with real values.

# Backend JSON API base URL (no trailing slash)
# Real API calls are disabled until an approved browser-safe authentication
# mechanism exists. The frontend currently uses the documented fixture adapter.
NEXT_PUBLIC_API_URL=http://localhost:3001
